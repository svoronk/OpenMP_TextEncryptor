This is a C++ project for my Parallel Programming course. It’s similar to my other lab, but instead of using C# Monitors, I used OpenMP to split up the work across my CPU cores.

The goal was to take a text file, run a simple Caesar-style encryption/decryption on the lines, and save the filtered results into a formatted table.
